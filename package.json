{
  "name": "root",
  "workspaces": [
    "packages/*"
  ],
  "type": "commonjs",
  "devDependencies": {
    "lerna": "^5.5.0"
  },
  "scripts": {
    "clean": "lerna clean",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap --hoist",
    "test": "lerna run test"
  }
}
